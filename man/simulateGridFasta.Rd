% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulateGridFasta}
\alias{simulateGridFasta}
\title{Simulate Grid from FASTA File(s)}
\usage{
simulateGridFasta(
  fasta_file,
  rbp_models,
  protein_conc_grid,
  rna_conc_grid,
  k = 5,
  parallel = TRUE,
  n_cores = NULL
)
}
\arguments{
\item{fasta_file}{Character vector of path(s) to FASTA files.}

\item{rbp_models}{Named list of RBP models.}

\item{protein_conc_grid}{List of numeric vectors for each RBP concentration.}

\item{rna_conc_grid}{Numeric vector of RNA concentrations to sweep.}

\item{k}{K-mer size (default 5).}

\item{parallel}{Logical, whether to use parallel processing (default TRUE).}

\item{n_cores}{Integer number of cores. If NULL (default), defaults to detectCores() - 1.}
}
\value{
A data.table with combined results, including a transcript column.
}
\description{
Simulates competitive binding for all sequences in one or more FASTA files across a concentration grid.
}
\examples{
model_file <- system.file("extdata", "model_RBP.csv", package = "RBPEqBind")
fasta_file <- system.file("extdata", "test_transcripts.fa", package = "RBPEqBind")
rbp_models <- setModel(loadModel(model_file, rbp = c("HH", "HL")))
results <- simulateGridFasta(
  fasta_file = fasta_file,
  rbp_models = rbp_models,
  protein_conc_grid = list(HH = c(10, 100), HL = c(10, 100)),
  rna_conc_grid = 10,
  k = 5,
  parallel = FALSE
)
head(results)
}
